# P2P tarefas 01–03 — plano de implementação

> **Para agentes:** usar `superpowers:executing-plans` (ou subagent-driven-development) tarefa a tarefa. Checkboxes `- [ ]` para acompanhamento.

**Objetivo:** Corrigir imports e protocolo da eleição UDP legada e remover código morto no master, sem alterar o fluxo principal já implementado.

**Arquitetura:** Constantes de configuração alinhadas com `election.py`; funções de mensagem em `protocol.py` compatíveis com `worker.py`; um único loop de aceitação no master.

**Stack:** Python 3, sockets, threading.

---

### Task 01: Constantes de config para `election.py`

**Ficheiros:**
- Modificar: `config.py`
- Verificar: `python -c "import election"`

- [ ] **Passo 1:** Abrir `config.py` e acrescentar após o bloco de eleição existente:

```python
# Usados por election.py (eleição TCP/consenso — módulo preparado para evolução)
ELECTION_SERVER_PORT = 5030       # TCP: anúncios de candidatos (distinto do UDP ELECTION_PORT)
ELECTION_MASTER_PORT = MASTER_PORT
HEARTBEAT_FAIL_THRESHOLD = MAX_HEARTBEAT_FAILS
```

- [ ] **Passo 2:** Executar verificação:

Run: `python -c "import election"`
Expected: sem `ImportError`

- [ ] **Passo 3:** Commit (opcional)

```bash
git add config.py
git commit -m "fix(config): add election module constants for import compatibility"
```

---

### Task 02: Mensagens `election_bid` e `election_victory`

**Ficheiros:**
- Modificar: `protocol.py`
- Referência consumidora: `worker.py` (linhas com `election_bid`, `election_victory`; handler `ELECTION_BID` / `ELECTION_VICTORY`)

- [ ] **Passo 1:** Em `protocol.py`, após as funções `i_am_master` (ou junto ao bloco de eleição), adicionar:

```python
def election_bid(worker_id, free_space, ip, port):
    """Broadcast UDP: candidatura na eleição legada (worker.py)."""
    return {
        "type": "ELECTION_BID",
        "worker_id": worker_id,
        "free_space": free_space,
        "ip": ip,
        "port": port,
    }


def election_victory(worker_id, ip, port):
    """Broadcast UDP: anúncio do vencedor da eleição legada."""
    return {
        "type": "ELECTION_VICTORY",
        "worker_id": worker_id,
        "ip": ip,
        "port": port,
    }
```

- [ ] **Passo 2:** Verificar imports:

Run: `python -c "from protocol import election_bid, election_victory; print(election_bid('w', 1, '127.0.0.1', 5000))"`
Expected: impressão de dict com `"type": "ELECTION_BID"`

- [ ] **Passo 3:** Commit (opcional)

```bash
git add protocol.py
git commit -m "fix(protocol): add UDP election_bid and election_victory helpers"
```

---

### Task 03: Limpeza do `master.py`

**Ficheiros:**
- Modificar: `master.py`

- [ ] **Passo 1:** Remover a função `create_connections` completa (definição e docstring associada se existir).

- [ ] **Passo 2:** Confirmar que apenas `start_server` contém o loop `server.accept()` + `Thread(..., handle_worker, ...)`.

- [ ] **Passo 3:** Verificação sintática:

Run: `python -m py_compile master.py`
Expected: exit code 0

- [ ] **Passo 4:** Commit (opcional)

```bash
git add master.py
git commit -m "refactor(master): remove unused duplicate accept helper"
```

---

## Cobertura da especificação

| Requisito no spec | Task |
|-------------------|------|
| Constantes `election.py` | 01 |
| Mensagens UDP legadas | 02 |
| Código morto master | 03 |

## Auto-revisão

- Sem placeholders TBD nas tarefas acima.
- Caminhos de ficheiro explícitos.
- Nenhuma alteração pedida ao `worker.py` neste plano (reduz regressão).
